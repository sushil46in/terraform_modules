/*This terraform file has been generated programmatically using terraform-generator.*/
/*All the commented lines, if any, are optional. Remove comment characters, if required, before using.*/
/*Refer https://github.com/sushil46in/terraform_modules.git for more details.*/

resource "alicloud_actiontrail" "resname" {
  #event_rw = var.actiontrail_event_rw
  #is_organization_trail = var.actiontrail_is_organization_trail
  #mns_topic_arn = var.actiontrail_mns_topic_arn
  #oss_bucket_name = var.actiontrail_oss_bucket_name
  #oss_key_prefix = var.actiontrail_oss_key_prefix
  #oss_write_role_arn = var.actiontrail_oss_write_role_arn
  #sls_project_arn = var.actiontrail_sls_project_arn
  #status = var.actiontrail_status

  timeouts {
    #create = var.actiontrail_timeouts_create
    #update = var.actiontrail_timeouts_update
  }

}

