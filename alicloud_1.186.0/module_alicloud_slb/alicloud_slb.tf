/*This terraform file has been generated programmatically using terraform-generator.*/
/*All the commented lines, if any, are optional. Remove comment characters, if required, before using.*/
/*Refer https://github.com/sushil46in/terraform_modules.git for more details.*/

resource "alicloud_slb" "resname" {
  #address_ip_version = var.slb_address_ip_version
  #bandwidth = var.slb_bandwidth
  #delete_protection = var.slb_delete_protection
  #internet_charge_type = var.slb_internet_charge_type
  #modification_protection_reason = var.slb_modification_protection_reason
  #period = var.slb_period
  #tags = var.slb_tags
  #vswitch_id = var.slb_vswitch_id

  timeouts {
    #create = var.slb_timeouts_create
    #delete = var.slb_timeouts_delete
  }

}

